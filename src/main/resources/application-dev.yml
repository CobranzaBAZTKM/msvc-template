server:
  port: 9090
  servlet:
    context-path: /api/v1/msvc-template

spring:
  application:
    name: msvc-template
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
#  data:
#    mongodb:
#      database: msvc-template
#      host: localhost
#      port: 27017

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs
  show-actuator: true
